'use strict';

let raizCuadrada = () => {
    let numero = Number(prompt("Introduce un número para calcular su raíz cuadrada"));
    return Math.sqrt(numero);
};

console.log("La raiz cuadrada es: " + raizCuadrada());


